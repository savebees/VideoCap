# Eval deps. Heavy libs may already exist in a serving env; run_eval itself
# needs no torch (only clip_score.py / metaclip2_score.py do).
openai>=1.30
pyyaml>=6.0
numpy<2
decord>=0.6
pillow>=9.0
spacy==3.7.5
# spaCy model, installed separately: python -m spacy download en_core_web_sm
# clip_score.py / metaclip2_score.py additionally need: torch, transformers
