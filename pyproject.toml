[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "videocap"
version = "0.1.0"
description = "Production-oriented dense video captioning and quality evaluation"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
dependencies = [
  "jsonschema>=4.0.0",
  "PyYAML>=6.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[dependency-groups]
dev = ["pytest>=7.0"]

[tool.uv]
default-groups = ["dev"]

[project.scripts]
videocap = "videocap.cli:main"

[tool.setuptools.packages.find]
include = ["videocap*"]

[tool.setuptools.package-data]
"videocap.schemas" = ["*.json"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]
